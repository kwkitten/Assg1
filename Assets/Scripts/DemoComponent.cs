using UnityEngine;

public class DemoComponent : MonoBehaviour
{
    // variables should be here
    // int health = 10;
    // int maxHealth = 50;
    // float speed = 5.0f;
    // string demoString = "Ayam Penyet from Food Club is the best";
    // bool isAlive = true;
    // string number = " ";
    // int smaller = 0;
    // int larger = 10;
    // int e = 0;

    // void OnCollisionEnter(Collision collision)
    // {
    //     Debug.Log("Collision detected with " + collision.gameObject.name);
    // }

    

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        // Debug.Log(CalculateArea(5.0f, 10.0f));
        // Debug.Log(CalculateArea(3.2f, 6.8f));
        // Debug.Log(CalculateArea(2.5f, 4.5f));

        // Debug.Log("Are you sure " + demoString);
        // Debug.Log(isAlive);

        // for(int i = 0; i < 10; ++i )
        // {
        //     --health;
        // }

        // while(health > 0)
        // {
        //     --health
        // }


        // if(health >= maxHealth)
        // {
        //     Debug.Log("Player is at full health");
        // }
        // else if(health < maxHealth && health > 0)
        // {
        //     Debug.Log("Player is not at full health");
        // }
        // else if(health == 0)
        // {
        //     Debug.Log("Player is dead");
        // }

        // if(health % 2 == 0)
        // {
        //     Debug.Log("Health is even");
        // }
        // else{
        //     Debug.Log("Health is odd");
        // }
        // for(int i = 1; i < 11; ++i)
        // {
        //     number += i + " ";
        // }
        // Debug.Log(number);

        // while(smaller < larger)
        // {
        //     ++e;
        //     ++smaller;
        // }
        // Debug.Log("It took " + e + " increments to make the numbers equal");
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("This is a demo component");
    }
}
